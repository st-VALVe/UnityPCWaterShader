# This workflow has been disabled — CodeQL is not needed for this inactive repository.
# To re-enable, restore the original triggers (push, pull_request, schedule).
name: CodeQL Security Analysis (disabled)

on:
  workflow_dispatch: # manual trigger only

jobs:
  placeholder:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
